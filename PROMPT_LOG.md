# Prompt Log

## Задание 1: Определение агентов и их ролей
**Инструмент:** opencode (gemma-4-31b-it)

### Промпт 1
**Промпт:** "В рамках реализации мультиагентной системы по предметной области «Борьба с мошенничеством» я выделил следующих агентов: 1) Сбор транзакций, 2) Анализ паттернов, 3) Оценка риска, 4) Блокировка. Опиши роль каждого агента, определи структуру входных и выходных данных, а также сформулируй бизнес-правила обработки. Дополнительно необходимо спроектировать структуру проекта и подготовить `.gitignore` для исключения артефактов сборки и виртуальных окружений."

**Результат:** Разработано подробное описание функциональных ролей четырёх агентов с детализацией правил валидации, алгоритмов расчета риск-скора и спецификаций JSON-пакетов. Спроектирована и развёрнута иерархия директорий проекта (`agent/`, `orchestrator/`, `api/`, `docs/`). Сконфигурирован `.gitignore` в строгом соответствии с требованиями по чистоте репозитория (исключение бинарных файлов и окружений). Подробная документация по архитектуре агентов вынесена в `docs/task1.md`.

### Итого
- Количество промптов: 1
- Что пришлось исправлять вручную: заменены LaTeX-стрелки (`$\rightarrow$`, `\rightarrow`) и математическая разметка (`$>$`, `$<$`, `\le`, `\min`, `\sum`) на читаемые символы (`→`, `>`, `<`) в `docs/task1.md` после повторной записи файла.
- Время: ~15 мин.

---

## Задание 2: Разработка прототипа агента на Go
**Инструмент:** opencode (gemma-4-31b-it)

### Промпт 1
**Промпт:** "Для реализации прототипа агента на Go необходимо развернуть инфраструктуру брокера сообщений NATS. Создай базовый `docker-compose.yml` для запуска сервера NATS с открытыми портами 4222 и 8222."

**Результат:** Создан конфигурационный файл `docker-compose.yml`, позволяющий развернуть сервер NATS в Docker-контейнере. Брокер доступен по стандартному порту 4222, настроен порт мониторинга 8222. Инфраструктура готова к подключению Go-агентов.

### Промпт 2
**Промпт:** "Реализуй функционал агента 'Оценка риска' в папке `agent/`. Код должен включать: инициализацию Go-модуля, установку зависимости nats.go, определение структур данных для запроса и ответа, алгоритм расчета риск-скора на основе весов маркеров и логику подписки на очередь NATS `tasks.risk_assessment` с использованием Queue Group для балансировки. Обеспечь строгую проверку всех ошибок и полное отсутствие комментариев в коде."

**Результат:** Инициализирован Go-модуль `lab13/agent`. Создан файл `main.go` с реализацией бизнес-логики оценки рисков и транспортного слоя на базе NATS. Внедрена система весов для маркеров мошенничества и механизм автоматического определения вердикта (LOW/MEDIUM/HIGH). Реализована поддержка горизонтального масштабирования через Queue Group `risk_assessors`. Все возвращаемые ошибки обрабатываются в соответствии с идиомами Go.

### Промпт 3
**Промпт:** "Реализуй тестирование Go-агента, используя подход Table-Driven subtests для всех сценариев расчета риска (границы вердиктов, неизвестные маркеры, экстремальные значения). Добавь интеграционный тест JSON Pipeline для проверки корректности сериализации данных. Пиши код без комментариев."

**Результат:** Создан файл `main_test.go` с расширенным набором тестов. Реализованы подтесты для всех граничных значений риск-скора (30, 31, 80, 81) и проверка устойчивости к некорректным маркерам. Добавлен тест полной цепочки сериализации/десериализации JSON. Из всех файлов исходного кода и тестов удалены комментарии. Все тесты проходят успешно.

### Промпт 4
**Промпт:** "Создай Dockerfile для Go-агента с использованием multi-stage build. Убедись, что образ минимален и безопасен. Проверь работоспособность агента в Docker-контейнере, запустив его в сети с сервером NATS."

**Результат:** Создан оптимизированный `Dockerfile` на базе `golang:1.26-alpine` и `alpine:latest`. Внедрена поддержка переменной окружения `NATS_URL` для гибкой настройки адреса сервера. Проверена успешная сборка образа `lab13-risk-assessor` и запуск агента в сети `lab13_default`, подтвержденный логом `Risk Assessor agent is running...`.

### Итого
- Количество промптов: 4
- Что пришлось исправлять вручную: внедрена поддержка переменной окружения `NATS_URL` для корректного подключения к брокеру в Docker; исправлены сообщения коммитов для точного соответствия внесенным изменениям; удалены все комментарии из исходного кода и тестов.
- Время: ~40 мин.

---

## Задание 3: Разработка оркестратора на Python
**Инструмент:** opencode (gemma-4-31b-it)

### Промпт 1
**Промпт:** "Разработай базовую архитектуру оркестратора на Python. Используй библиотеку asyncio и nats-py. Реализуй класс AgentOrchestrator с методами для подключения к NATS, подписки на тему ответов `tasks.completed` и отправки задач в тему `tasks.risk_assessment` с использованием asyncio.Future для ожидания ответа. Обеспечь строгое использование type hints и полное отсутствие комментариев в коде."

**Результат:** Созданы файлы `orchestrator/requirements.txt` и `orchestrator/orchestrator.py`. Реализован асинхронный класс `AgentOrchestrator`, обеспечивающий отправку задач и получение результатов через механизм Future. Внедрена поддержка уникальных идентификаторов задач (UUID) для корректного сопоставления запросов и ответов в распределенной среде.

### Промпт 2
**Промпт:** "Проведи рефакторинг оркестратора: вынеси темы NATS в константы, замени Dict[str, Any] на TypedDict для запросов и ответов, добавь базовую валидацию входных данных (проверка markers на список). Создай точку входа orchestrator/main.py для интеграционного тестирования с отправкой серии сценариев (Low, Medium, High risk) и выводом результатов в консоль. Пиши код без комментариев с использованием type hints."

**Результат:** Оптимизирована структура `orchestrator.py` за счёт внедрения типизированных словарей и констант, что повысило устойчивость кода к ошибкам. Реализован скрипт `main.py`, позволяющий верифицировать сквозную работу системы «Оркестратор $\rightarrow$ NATS $\rightarrow$ Go-агент» на различных бизнес-сценариях.

### Промпт 3
**Промпт:** "Реализуй полное тестовое покрытие для оркестратора в orchestrator/tests/test_orchestrator.py. Используй подход Table-Driven (pytest.mark.parametrize) для проверки всех сценариев отправки задач и обработки результатов. Обеспечь проверку корректности вызова nats.connect, валидацию publish в NATS, проверку очистки словаря results (state cleanup) и обработку всех пограничных случаев (таймауты, невалидный JSON, отсутствие соединения). Код должен быть без комментариев и заглушек."

**Результат:** Создан расширенный набор модульных тестов с использованием pytest-asyncio и unittest.mock. Реализована матрица проверок для каждого метода класса AgentOrchestrator, включая стресс-тесты на некорректные данные. Достигнуто полное покрытие критических путей исполнения, подтверждено отсутствие утечек памяти в реестре задач.

### Промпт 4
**Промпт:** "Проведи ревью кода на предмет основных проблем: проверь обработку ошибок в Go, кросс-платформенную совместимость, стиль кода, корректность go.mod, тестовое покрытие и .gitignore. Найденные дефекты исправь."

**Результат:** Проведён аудит кодовой базы. Найдены и исправлены следующие проблемы: зажатие риск-скора в диапазон [0, 100] в agent/main.go для предотвращения отрицательных значений, замена syscall.SIGINT/SIGTERM на os.Interrupt для кросс-платформенной совместимости с Windows, исправление go.mod через go mod tidy (все зависимости были отмечены indirect, хотя nats.go используется напрямую), удаление комментария type: ignore и замена на cast, замена @pytest.fixture на @pytest_asyncio.fixture в тестах для совместимости с pytest-asyncio strict-mode, удаление неиспользуемого импорта nats, исправление race condition в test_send_task_success через polling-цикл, добавление *.pyc в .gitignore.

### Итого
- Количество промптов: 4
- Что пришлось исправлять вручную: дважды исправлялась склейка строк импорта (castimport nats); заменён тип msg с nats.Msg на Msg из nats.aio.msg из-за отсутствия экспорта в корневом модуле nats-py; переписаны импорты для Dict, Optional, Any, List, TypedDict, cast после чистки неиспользуемых.
- Время: ~60 мин.

---

## Задание 4: Настройка коммуникации через NATS
**Инструмент:** opencode (gemma-4-31b-it)

### Промпт 1
**Промпт:** "Добавь сервис Go-агента в docker-compose.yml со сборкой из ./agent, переменной NATS_URL=nats://nats:4222 и depends_on nats. Запусти стек, проверь логи агента. Затем запусти orchestrator/main.py и убедись, что все 4 сценария (Low, Medium, High, Invalid) отрабатывают корректно."

**Результат:** Стек развёрнут в Docker. Агент успешно стартовал (лог Risk Assessor agent is running...). Оркестратор отправляет задачи в NATS, они обрабатываются Go-агентом, результаты возвращаются корректно для всех сценариев: LOW (0), MEDIUM (55), MEDIUM (80 как пограничный случай), ошибка валидации для некорректного payload. Сквозная коммуникация через NATS полностью подтверждена.

### Итого
- Количество промптов: 1
- Что пришлось исправлять вручную: —
- Время: ~15 мин.

---

## Задание 5: Логирование и мониторинг
**Инструмент:** opencode (big-pickle)

### Промпт 1
**Промпт:** "Добавь в Go-агента структурированное логирование с уровнями INFO/ERROR, счётчик обработанных задач processedTasks и вывод финальной статистики при завершении работы."

**Результат:** В agent/main.go добавлены префиксы `INFO:` и `ERROR:` во все log.Printf, введена глобальная переменная `processedTasks` с инкрементом после каждой успешной публикации, добавлен вывод `INFO: Agent shutting down. Total tasks processed: N` в блоке завершения. Go-тесты проходят успешно.

### Промпт 2
**Промпт:** "Добавь в Python-оркестратор структурированное логирование с уровнями INFO/ERROR, счётчик обработанных задач и запись в файл + консоль."

**Результат:** В `orchestrator/orchestrator.py` добавлен module-level логгер, счетчик `self.processed`, логи уровня INFO/ERROR во всех методах. В `orchestrator/main.py` настроен `logging.basicConfig` с `FileHandler` (режим `w`) и `StreamHandler`. 15 pytest-тестов проходят.

### Промпт 3
**Промпт:** "Добавь логирование в файл для Go-агента, настрой оркестратор для работы в Docker, обеспечь обработку сигналов и корректное логирование."

**Результат:** В `agent/main.go` добавлен `io.MultiWriter(os.Stdout, logFile)` — лог дублируется в консоль и `agent.log`. Создан `orchestrator/Dockerfile` на базе `python:3.13-slim`, зафиксированы версии зависимостей (`nats-py==2.14.0`, `pytest==8.4.2`, `pytest-asyncio==1.3.0`). В `docker-compose.yml` добавлен сервис `orchestrator` с `NATS_URL=nats://nats:4222`. В `orchestrator/main.py` добавлено чтение `NATS_URL` из окружения, обработка `SIGINT`/`SIGTERM` через `add_signal_handler` с graceful disconnect. В `agent/main.go` добавлен `syscall.SIGTERM` в `signal.Notify`. Созданы `.dockerignore` для обоих сервисов. Docker-образы собираются и работают корректно.

### Промпт 4
**Промпт:** "Улучши тестовое покрытие: добавь тесты для counter'ов, listener'а, логирования в Python, граничные случаи в Go, убери исключение тестов из .dockerignore."

**Результат:** В `orchestrator/tests/test_orchestrator.py` добавлено 6 новых тестов: `test_processed_counter_increments`, `test_disconnect_with_processed`, `test_start_listener_subscribes_correctly`, `test_connect_logs_url`, `test_send_task_logs_start_and_complete`, `test_disconnect_logs_processed`. Общее количество тестов увеличено с 15 до 21. В `agent/main_test.go` добавлены 2 крайних случая: переполнение confidence (>1.0) и пустой TransactionID. Общее количество табличных случаев — 13. Из `.dockerignore` удалены `tests/` (orchestrator) и `*_test.go` (agent). Тесты проходят как локально, так и в Docker.

### Итого
- Количество промптов: 4
- Что пришлось исправлять вручную: дублирование логов из-за instance-level логгера (замена на module-level с basicConfig), флуд ERROR/WARNING в on_result (понижен до DEBUG), FileHandler с append на write, restart оркестратора с always на "no", обёртка add_signal_handler в try/except для Windows, лишний пробел в декораторе `@ pytest.mark.asyncio`.
- Время: ~60 мин.

---

## Задание 6: Обработка ошибок и таймаутов
**Инструмент:** opencode (big-pickle)

### Промпт 1
**Промпт:** "Добавь retry-логику в оркестратор: оберни send_task в цикл с MAX_RETRIES=3, TimeoutError → retry с новым uuid, Exception → retry, ValueError/ConnectionError — без retry."

**Результат:** В `orchestrator/orchestrator.py` добавлена константа `MAX_RETRIES = 3`. Метод `send_task` переписан: валидация вынесена перед циклом (без retry), каждая попытка — новый `task_id` + `Future`, `TimeoutError` и `Exception` логируются как WARNING и повторяются, после исчерпания попыток пробрасывается последняя ошибка. Исправлена опечатка `SUBSCRIPT_` → `SUBJECT_`. 21 существующий pytest и 15 Go-тестов проходят без изменений.

### Промпт 2
**Промпт:** "Добавь тесты retry-логики и выполни Docker-проверку: 4 теста (success на второй попытке, exhaustion после MAX_RETRIES, без retry на ValueError, подсчёт WARNING-логов) + прогон всех 4 сценариев в Docker."

**Результат:** В `orchestrator/tests/test_orchestrator.py` добавлено 6 новых тестовых случаев (включая 3 параметризованных для validation error): `test_retry_success_on_second_attempt` (publish падает на первом вызове), `test_retry_exhaustion` (3 TimeoutError → raise), `test_retry_no_retry_on_validation_error` (3 варианта), `test_retry_logs_warning_on_each_retry` (3 WARNING). В `orchestrator/__init__.py` добавлен экспорт `MAX_RETRIES`. В `orchestrator/main.py` добавлен 5-й сценарий `Retry Exhaustion (timeout 1ms)` с поддержкой `timeout` в сценарии. Docker: образы собраны, стек запущен, все 5 сценариев выполнены — первые 3 успешно, Invalid с ошибкой валидации, Retry Exhaustion с 3 WARNING + финальной ошибкой после 3 попыток. Всего 27 pytest + 15 Go-тестов проходят.

### Итого
- Количество промптов: 2
- Что пришлось исправлять вручную: добавлен экспорт `MAX_RETRIES` в `__init__.py` (константа не была доступна для импорта в тестах).
- Время: ~30 мин.

## Задание 7: Запуск нескольких агентов одного типа
**Инструмент:** opencode (big-pickle)

### Промпт 1
**Промпт:** "Реализуй AGENT_ID в Go-агенте (префикс в логах), 3 экземпляра агента в docker-compose.yml через YAML anchor, batch-сценарий на 6 задач в main.py с поддержкой repeat."

**Результат:** В `agent/main.go` добавлено чтение `AGENT_ID` из окружения (по умолчанию "agent"), логгеры пишут с префиксом `[agentID]`. В `docker-compose.yml` добавлены `agent-1`, `agent-2`, `agent-3` через YAML anchor `x-agent`. В `orchestrator/main.py` добавлен 6-й сценарий `Batch (6 tasks for multi-agent)` с полем `repeat`, цикл отправки переписан для поддержки повторений. Docker: все 3 агента запущены, задачи распределяются через NATS Queue Group (batch 6 задач разделились 2+2+2 по агентам). 27 pytest + 15 Go-тестов проходят без изменений.

### Промпт 2
**Промпт:** "Добавь тесты для нескольких агентов: unit-тест с параллельными задачами (asyncio.gather) и e2e Docker-тест с проверкой распределения по 3 агентам."

**Результат:** В `orchestrator/tests/test_orchestrator.py` добавлен `test_concurrent_tasks` — 3 параллельные задачи через `asyncio.gather`, проверка `processed == 3` и пустого registry. Создан `orchestrator/tests/test_multi_agent.py` с e2e Docker-тестом `test_multi_agent_load_balancing` — запуск 3 агентов, отправка 9 задач, проверка логов всех трёх агентов, cleanup через `docker compose down`. Всего 29 pytest + 15 Go-тестов проходят.

### Промпт 3
**Промпт:** "Проведи рефакторинг тестов оркестратора: вынеси общую фикстуру `orchestrator` и хелпер `resolve_futures` в `conftest.py`, параметризуй существующие тесты (`test_send_task_success`, `test_send_task_timeout`, `test_on_result_valid`), добавь новые тесты для покрытия граничных случаев (`test_on_result_already_done`, `test_disconnect_when_not_connected`, `test_send_task_additional_invalid`, `test_no_messages_lost`), реструктурируй `test_multi_agent.py` в класс с module-scope фикстурами."

**Результат:** Создан `orchestrator/tests/conftest.py` с общей фикстурой `orchestrator` и хелпером `resolve_futures`. `test_orchestrator.py` — параметризовано 3 существующих теста, добавлено 3 новых тестовых случая (`test_on_result_already_done`, `test_disconnect_when_not_connected`, `test_send_task_additional_invalid`), повышено покрытие граничных ситуаций. `test_multi_agent.py` — рефакторинг в класс `TestMultiAgent` с module-scope фикстурами `docker_stack` и `event_loop`, параметризация `test_load_balancing` (3 и 6 задач), добавлен `test_no_messages_lost`. Добавлена адаптация multi-agent тестов для запуска внутри Docker-контейнера (определение `inside_docker` через `/.dockerenv`, переключение `NATS_URL` на `nats://nats:4222`, пропуск управления Docker стеком при отсутствии CLI). Все тесты проходят: 40 pytest (37 unit + 3 e2e) + 15 Go — локально и в Docker (55/55).

### Итого
- Количество промптов: 3
- Что пришлось исправлять вручную: исправлена flaky проверка `assert agents_seen == {"agent-1", "agent-2", "agent-3"}` для случая 3 задач (Queue Group не гарантирует распределение на всех агентов при малом количестве); добавлено автоопределение `inside_docker` через `/.dockerenv` для корректного `NATS_URL` и пропуска Docker CLI-зависимых операций внутри контейнера; исправлена фикстура `docker_stack` — обязательный `yield` для совместимости с `pytest_asyncio`; дополнен `agent/.dockerignore` (исключены `agent.log` и `*.md` из контекста сборки); удалён мёртвый код в `orchestrator.py` (первая генерация UUID вне цикла retry никогда не использовалась).
- Время: ~30 мин.

---

## Задание 8: Создание API для запуска задач
**Инструмент:** opencode (big-pickle)

### Промпт 1
**Промпт:** "Реализуй FastAPI-приложение для запуска задач через REST API. Эндпоинты: POST /assess (синхронная оценка), POST /assess/async (асинхронная с task_id), GET /status/{task_id} (проверка статуса), POST /assess/batch (массовая отправка), GET /health (healthcheck). Добавь request_id в логи, rate limiting (10 req/min), Pydantic-валидацию. Подключение к NATS через lifespan."

**Результат:** Созданы `api/main.py` и `api/requirements.txt`. FastAPI-приложение с AgentOrchestrator через lifespan. Pydantic-модели для входа/выхода. Асинхронные задачи через in-memory storage + asyncio.create_task. Middleware для request_id и длительности запросов. Rate limiter на основе sliding window.

### Промпт 2
**Промпт:** "Создай Docker-инфраструктуру для API: api/Dockerfile, api/.dockerignore, обновление docker-compose.yml с сервисом api на порту 8000."

**Результат:** Создан `api/Dockerfile` (python:3.13-slim, копирование api/ и orchestrator/, PYTHONPATH=/app), `api/.dockerignore` (__pycache__, *.pyc, .pytest_cache). Обновлён `docker-compose.yml`: добавлен сервис `api` (build context = ., dockerfile = api/Dockerfile, порт 8000, depends_on: nats). Все эндпоинты проверены через curl.exe: GET /health → {"status":"ok"}, POST /assess → {"risk_score":80,...}, POST /assess/async → {"task_id":"...","status":"pending"}, POST /assess/batch → {"results":[...]}, GET /status/{task_id} → {"status":"completed","result":{...}}.

### Промпт 3
**Промпт:** "Разработай комплексное тестовое покрытие для FastAPI-приложения с использованием pytest и TestClient. Реализуй параметризованные тесты через pytest.mark.parametrize для всех эндпоинтов, создай переиспользуемые фикстуры в conftest.py (мокирование AgentOrchestrator через AsyncMock, подмена lifespan для отключения NATS), покрой негативные сценарии — невалидные JSON-тела запросов, неправильные HTTP-методы, таймауты и внутренние ошибки оркестратора, отсутствующие task_id, превышение rate limit. TestClient должен работать без реального подключения к NATS."

**Результат:** Созданы `api/__init__.py`, `api/tests/__init__.py`, `api/tests/conftest.py` и `api/tests/test_api.py` — 35 тестов. В conftest.py реализованы фикстуры mock_orch (AsyncMock) и client (no-op lifespan), автозапуск reset_state. Покрыты все эндпоинты: health, assess с параметризацией и негативными сценариями (невалидные тела, таймауты, ошибки), async, status (completed/pending/failed/not found), batch, rate limit. В ходе тестирования добавлена Pydantic-валидация confidence в диапазон [0,1]. Все 90 тестов (40 orchestrator + 35 API + 15 Go) проходят локально и в Docker.

### Итого
- Количество промптов: 3
- Что пришлось исправлять вручную: удалён мёртвый параметр `client_ip` из `check_rate_limit()` и `fastapi_request` из эндпоинтов; тип `orchestrator` изменён на `Optional[AgentOrchestrator]`; в `/assess/batch` добавлена обработка ValueError → 422 и TimeoutError → 504; удалён неиспользуемый импорт `Union`; добавлен `api/__init__.py` для импорта модуля как пакета; в conftest.py заменён `app.lifespan` на `app.router.lifespan_context` из-за изменения API в Starlette 0.40+; добавлен `Field(ge=0.0, le=1.0)` для валидации confidence; удалён тест CORS (не поддерживается без middleware).
- Время: ~40 мин.
